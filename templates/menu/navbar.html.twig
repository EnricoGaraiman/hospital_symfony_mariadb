<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light dashboard-navbar">
    <div class="container-fluid">

        <div id="sidebarCollapse">
            <i class="fas fa-bars fa-2x"></i>
        </div>
        <button class="btn btn-dark d-inline-block d-lg-none ml-auto" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-align-justify"></i>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="nav navbar-nav ms-auto">
                <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="javascript:void(0)">Salut, {{ app.user.prenumeMedic }} <span class="badge rounded-pill bg-success">ADMIN</span></a>
                    {% elseif is_granted('ROLE_MEDIC')%}
                        <a class="nav-link" href="javascript:void(0)">Salut, {{ app.user.prenumeMedic }}</a>
                    {% elseif is_granted('ROLE_PACIENT')%}
                        <a class="nav-link" href="javascript:void(0)">Salut, {{ app.user.prenumePacient }}</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link {{ app.request.get('_route') == 'medic_profile' ? 'active-link' }}"
                           href="{{ path('medic_profile', {'id': app.user.id}) }}"><i class="fa fa-user"></i> Profilul meu</a>
                    {% elseif is_granted('ROLE_MEDIC')%}
                        <a class="nav-link {{ app.request.get('_route') == 'medic_profile' ? 'active-link' }}"
                           href="{{ path('medic_profile', {'id': app.user.id}) }}"><i class="fa fa-user"></i> Profilul meu</a>
                    {% elseif is_granted('ROLE_PACIENT')%}
                        <a class="nav-link {{ app.request.get('_route') == 'pacient_profile' ? 'active-link' }}"
                           href="{{ path('pacient_profile', {'id': app.user.id}) }}"><i class="fa fa-user"></i> Profilul meu</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}"><i class="fa fa-sign-out-alt"></i> Ieși din cont</a>
                </li>
            </ul>
        </div>
    </div>
</nav>